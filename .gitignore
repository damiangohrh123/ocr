__pycache__/
*.pyc
*.pyo

# Benchmark output directories
annotated/
crops/

# OS
.DS_Store
Thumbs.db

# Large binary model files — track with Git LFS or download separately
# Uncomment these if you do NOT want to commit them directly:
# model/*.onnx
# model/*.rknn

# Confidential documents
*.docx

# Internal transition/handover notes — not for the public repo
transition.md

# Cached aarch64 cross-compile toolchain (local build cache, not source --
# see the "Reusing the Cached Cross-Toolchain" section of the top-level
# README.md)
cross_toolchain/*.tar.gz

# Visual Studio local IDE state / CMake integration -- machine-local,
# fully regenerable, not part of the documented command-line build.
.vs/
out/
CMakeSettings.json
CppProperties.json
